package frc.robot.subsystems.transport;

/**
 * No-op implementation of TransportIO used in simulation or replay.
 * All inputs default to zero; motor accessors return VoidMotors via the interface defaults.
 */
public class TransportIOSim implements TransportIO {
    // All methods use the default no-op implementations from TransportIO.
}
