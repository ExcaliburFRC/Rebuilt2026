package frc.robot.subsystems.turret;

/**
 * No-op implementation of TurretIO used in simulation or replay.
 * All inputs default to zero; motor accessors return VoidMotors via the interface defaults.
 */
public class TurretIOSim implements TurretIO {
    // All methods use the default no-op implementations from TurretIO.
}
